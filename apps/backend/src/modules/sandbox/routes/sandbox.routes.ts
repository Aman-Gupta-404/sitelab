import { Router } from "express";
import { SandboxController } from "../controller/sandbox.controller.js";

// const router = Router();
const sandboxController = new SandboxController();

// router.get("/run", sandboxController.runCommand);
